class Point {
    x : AUTO_TYPE;
    y : AUTO_TYPE;
    init(n : Int, m : Int) : Point {
        {
            x <- n;
            y <- m;
        }
    };

    translate(x: Int, y: Int) : Point {
        # No es que poner aqui ;)
    };
};

class Program {
    step(p : AUTO_TYPE) { p.translate(1,1) };

    main() : Object {
        let p : AUTO_TYPE <- new Point(0,0) in {
            step(p) # Puede lanzar error semantico
        };
    };
};